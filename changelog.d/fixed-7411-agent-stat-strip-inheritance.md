- Fixed newly created agents inheriting ci-maintainer's CI/coverage stat strip — the shipped `deploy/data/agents/reviewer/stats.json` seed was a verbatim copy of ci-maintainer's, so any agent created under that name rendered the CI/release indicators plus a fabricated `COVERAGE 0% vs goal 91%`. The retired seed is removed and the CI/coverage strip (the repo-wide `health` stats plus the `coverage` metric) is now scoped to the CI owner on read, so every other agent — including a freshly created one, and one whose spoke already has the seeded file — renders no stat strip at all rather than numbers that were never measured for it (hivecommons/hive#7411).
