- Fixed newly created agents inheriting ci-maintainer's CI/coverage stat strip — the shipped `deploy/data/agents/reviewer/stats.json` seed was a verbatim copy of ci-maintainer's, so any agent created under that name rendered the CI/release indicators plus a fabricated `COVERAGE 0% / goal: 91%`. The retired seed is removed, `health`-sourced stats are scoped to the CI owner on read (repairing already-seeded spokes), and an unmeasured `pct`/`pct-bar` stat now renders `—` ("not measured") instead of a confident `0%` (hivecommons/hive#7411).
