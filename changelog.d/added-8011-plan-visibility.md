- The dashboard now shows which issues are connected to an epic and how that epic's plan is progressing: a planned issue's 📋 button on the Repositories card is replaced by a state chip (`⧗ planning`, `● N tasks · review`, `✓ done/total`, `⚠ stuck`) that opens the plan review for its epic, the plan-review modal shows who has each task and links its pull request, and the governor PLANNING tile names what is waiting on a person — a task list awaiting approval, or an epic queued for the architect for 12h with nothing built — instead of folding both into a count that reads as "working" ([#8011](https://github.com/hivecommons/hive/issues/8011)).
