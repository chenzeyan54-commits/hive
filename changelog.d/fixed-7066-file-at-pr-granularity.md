- Issue-filing guidance in every agent policy template now requires PR-granularity findings: multi-part epics must either be filed as N separate issues (or a tracking issue plus N children, each closeable by one PR with `Fixes #N`) or carry a machine-readable `- [ ]` task list, and a new task-list sweep closes hive-filed issues once every box is ticked — respecting the `hold` label, refusing to close human-filed issues, refusing to close prose issues with zero boxes, and ignoring checkboxes that appear inside fenced code blocks ([#7066](https://github.com/hivecommons/hive/issues/7066)).
